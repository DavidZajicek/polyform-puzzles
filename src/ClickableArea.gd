extends CollisionPolygon2D

var points: PackedVector2Array

#func _draw() -> void:
#	for point in points:
#		#draw_rect(Rect2(point.x, point.y, point.x + Globals.tile_size.x, point.y + Globals.tile_size.y), Color.GREEN)
#		#draw_colored_polygon(points, Color.GREEN)
#
